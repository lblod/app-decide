x-logging: &default-logging
  driver: 'json-file'
  options:
    max-size: '10m'
    max-file: '3'
services:
  annotation-review:
    image: lblod/annotation-review-service:0.0.6
    restart: always
    labels:
      - logging=true
    logging: *default-logging
    environment:
      DEFAULT_MU_AUTH_SCOPE: 'http://services.semantic.works/annotation-review-service'
      ANNOTATION_TARGET_TYPES: 'http://www.w3.org/ns/oa#Annotation,http://mu.semte.ch/vocabularies/ext/AnnotationTarget'
      TZ: Europe/Brussels
    volumes:
      - ../config/annotation-review:/config
  frontend-human-validator:
    image: lblod/frontend-decide-human-validator:0.0.13
    labels:
      - 'logging=true'
    restart: always
    logging: *default-logging
